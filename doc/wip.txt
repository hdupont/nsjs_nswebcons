> les commandes gerent leur outupt

Ce n'est pas a la console de recevoir un resultat et de l'afficher.
C'est a la commande d'afficher son resultat (peut-etre via un api.output();

> Execute est plutot onInput

command.onInput(input)

onInput(input) {
	faire quelque chose
}

Pour faire ses outuput la commande a besoin de creer des ligne d'output.
Donc c'est a deplacer de la console dans la commande.